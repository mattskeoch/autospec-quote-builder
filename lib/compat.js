export function isCompatible(item, vehicleId) { const c = item.compat?.vehicles; if (!c || c === '*' || (Array.isArray(c) && c.length === 0)) return true; return Array.isArray(c) && c.includes(vehicleId); }
