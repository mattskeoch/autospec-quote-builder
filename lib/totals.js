export function computeTotals(selected, map){ let price=0, weight=0; for(const it of selected){ const v=map?.[String(it.variantId)]; if(v?.price!=null) price+=+v.price; if(v?.weight!=null) weight+=+v.weight; } return { price, weightKg: Math.round((weight/1000)*10)/10 }; }
