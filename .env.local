# Next (server) will proxy to Cloudflare when these are set.
# Leave them blank while you’re still using the local stubs.
CF_ENRICH_URL=
CF_DRAFT_ORDER_URL=
NEXT_PUBLIC_SITE_URL=http://localhost:3000